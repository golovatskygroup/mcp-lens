# Binaries (built binary, not npm package)
/mcp-lens

# Local configs (may contain secrets)
config.yaml
.env

# OS
.DS_Store

# Node dependencies and build artifacts (npm packages)
node_modules/
dist/
*.tsbuildinfo
# Note: keep package-lock.json committed for CI (npm ci)
# (do not ignore)
bin/*.worktrees/
