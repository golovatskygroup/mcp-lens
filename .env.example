# Copy to ".env" (do not commit .env; it may contain secrets).

# --- Router (OpenRouter) ---
# Required for the `query` tool to work.
OPENROUTER_API_KEY=
MCP_LENS_ROUTER_MODEL=

# Optional router settings
MCP_LENS_ROUTER_BASE_URL=https://openrouter.ai/api/v1
MCP_LENS_ROUTER_TIMEOUT_MS=30000

# --- GitHub ---
# Used by local GitHub helpers (PR diff/files/commits/checks).
# Also mapped into the default upstream config.
GITHUB_TOKEN=

# Optional fallback (local GitHub client will use this if GITHUB_TOKEN is empty)
GITHUB_PERSONAL_ACCESS_TOKEN=

# --- Upstream MCP server (optional) ---
# If you don't set anything here, the repo defaults to the GitHub upstream.
# Built-in presets: github
MCP_LENS_PRESET=github

# Or override upstream launch explicitly:
# MCP_LENS_UPSTREAM_COMMAND=npx
# MCP_LENS_UPSTREAM_ARGS_JSON='["-y","@modelcontextprotocol/server-github"]'
# MCP_LENS_UPSTREAM_ENV_JSON='{"GITHUB_PERSONAL_ACCESS_TOKEN":"${GITHUB_TOKEN}"}'
MCP_LENS_UPSTREAM_COMMAND=
MCP_LENS_UPSTREAM_ARGS_JSON=
MCP_LENS_UPSTREAM_ENV_JSON=

# --- Jira (optional) ---
# Jira Server / Data Center (recommended for https://jira.webpower.ru)
JIRA_BASE_URL=https://jira.webpower.ru
JIRA_PAT=YOUR_JIRA_PERSONAL_ACCESS_TOKEN

# Jira (bearer token alternative)
# JIRA_BEARER_TOKEN=YOUR_BEARER_TOKEN
JIRA_BEARER_TOKEN=

# Jira Cloud (basic auth alternative)
# JIRA_BASE_URL=https://your-site.atlassian.net
# JIRA_EMAIL=you@example.com
# JIRA_API_TOKEN=YOUR_ATLASSIAN_API_TOKEN
JIRA_EMAIL=
JIRA_API_TOKEN=

# Jira Cloud OAuth 2.0 (3LO) (alternative)
# JIRA_CLOUD_ID=YOUR_CLOUD_ID
# JIRA_OAUTH_ACCESS_TOKEN=YOUR_OAUTH_ACCESS_TOKEN
JIRA_CLOUD_ID=
JIRA_OAUTH_ACCESS_TOKEN=

# Multi-client setup (optional): call `query` with prefix `jira <client>` (e.g. "jira webpower show GO-27").
# JIRA_CLIENTS_JSON='{
#   "webpower": {"base_url":"https://jira.webpower.ru","api_version":2,"pat":"..."},
#   "cloud": {"base_url":"https://your-site.atlassian.net","api_version":3,"email":"...","api_token":"..."}
# }'
# JIRA_DEFAULT_CLIENT=webpower
JIRA_CLIENTS_JSON=
JIRA_DEFAULT_CLIENT=

# --- Confluence (optional) ---
# Confluence Cloud (basic auth with email + API token)
# CONFLUENCE_BASE_URL=https://your-site.atlassian.net/wiki
# CONFLUENCE_EMAIL=you@example.com
# CONFLUENCE_API_TOKEN=YOUR_ATLASSIAN_API_TOKEN
CONFLUENCE_BASE_URL=
CONFLUENCE_EMAIL=
CONFLUENCE_API_TOKEN=

# Confluence (bearer token / PAT alternative; Cloud or Data Center depending on instance)
CONFLUENCE_BEARER_TOKEN=
CONFLUENCE_PAT=

# Confluence Data Center / Server (basic auth alternative; if enabled on your instance)
CONFLUENCE_USERNAME=
CONFLUENCE_PASSWORD=

# Confluence Cloud OAuth 2.0 (3LO) (alternative): uses https://api.atlassian.com/ex/confluence/${CONFLUENCE_CLOUD_ID}
CONFLUENCE_CLOUD_ID=
CONFLUENCE_OAUTH_ACCESS_TOKEN=

# Multi-client setup (optional): call `query` with prefix `confluence <client>` (e.g. "confluence docs find page ...").
# CONFLUENCE_CLIENTS_JSON='{
#   "cloud": {"base_url":"https://your-site.atlassian.net/wiki","email":"...","api_token":"..."},
#   "cloud_oauth": {"cloud_id":"...","oauth_access_token":"..."},
#   "dc": {"base_url":"https://confluence.company.com","pat":"..."}
# }'
# CONFLUENCE_DEFAULT_CLIENT=cloud
CONFLUENCE_CLIENTS_JSON=
CONFLUENCE_DEFAULT_CLIENT=

# --- Tests only (optional) ---
# Used by `go test -tags=e2e ./internal/tools/...` Jira checks.
# Set to a real issue key visible to your credentials.
JIRA_E2E_ISSUE_KEY=

# Used by integration tests that hit GitHub API via the local tools.
# Example: https://github.com/<owner>/<repo>/pull/<number>
GITHUB_E2E_PR_URL=
